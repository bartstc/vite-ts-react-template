// AIDEV-NOTE: Domain model re-exported from the API DTO — shape identical to the wire
// format (frontend-model rule: re-export when identical). See 003.
export type { Order } from "@/lib/api/checkout/order-dto";
